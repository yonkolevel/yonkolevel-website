import * as React from 'react';

const RicardoWalking: React.FunctionComponent = (props) => {
  return (
    // eslint-disable-next-line @next/next/no-img-element
    <img
      src='/images/illustrations/dev_walking.gif'
      alt='Ricardo Walking'
    />
  );
};

export default RicardoWalking;
