import * as React from 'react';

const RicardoIdle: React.FunctionComponent = (props) => {
  return (
    <img
      src='/images/illustrations/dev_idle.gif'
      alt='Ricardo Idle'
    />
  );
};

export default RicardoIdle;
